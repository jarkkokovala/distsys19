INITIALIZER:
  from Tkinter import *
  import WatchHardware
  import DWatchGUI
  root = Tk()
  t = root.title('Digital Watch')
  gui = DWatchGUI(root, eventhandler)
  watch = WatchHardware(eventhandler, gui)

  eventhandler.start()
  watch.start()

STATECHART:
  Initial[DS]